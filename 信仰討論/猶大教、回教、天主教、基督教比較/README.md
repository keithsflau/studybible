# 四大宗教比較分析網站

這是一個詳細比較猶太教、伊斯蘭教、天主教和基督教的網站。

## 功能特點

- 📚 詳細的歷史背景分析
- 🎯 核心信仰和教義比較
- 📖 核心經卷介紹
- 🎉 重要節期說明
- 🏛️ 教派來源與發展
- 💫 得救觀、神觀、末世觀分析
- 📊 綜合比較表格

## 技術特點

- 使用 Tailwind CSS 進行現代化設計
- 響應式設計，支援各種設備
- 使用 CDN，無需構建工具
- 可直接部署到 GitHub Pages

## 部署到 GitHub Pages

### 方法一：直接部署 HTML 文件

1. 將此文件夾推送到 GitHub 倉庫
2. 在 GitHub 倉庫設置中啟用 GitHub Pages
3. 選擇主分支（main 或 master）
4. 設置源文件夾為 `/bible_study/信仰討論/猶大教、回教、天主教、基督教比較/`
5. 訪問 `https://[你的用戶名].github.io/[倉庫名]/bible_study/信仰討論/猶大教、回教、天主教、基督教比較/`

### 方法二：使用 GitHub Actions（推薦）

創建 `.github/workflows/deploy.yml`：

```yaml
name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./bible_study/信仰討論/猶大教、回教、天主教、基督教比較
```

### 方法三：使用 gh-pages 工具

```bash
npm install -g gh-pages
gh-pages -d "bible_study/信仰討論/猶大教、回教、天主教、基督教比較"
```

## 本地預覽

直接在瀏覽器中打開 `index.html` 文件即可預覽，或使用本地服務器：

```bash
# Python 3
python -m http.server 8000

# Node.js (需要安裝 http-server)
npx http-server
```

然後訪問 `http://localhost:8000`

## 文件結構

```
猶大教、回教、天主教、基督教比較/
├── index.html          # 主頁面
└── README.md          # 說明文件
```

## 注意事項

- 本網站使用 Tailwind CSS CDN，需要網絡連接
- 所有內容均為客觀分析，僅供參考
- 如需修改內容，直接編輯 `index.html` 即可

